name: Update Birthday Messages

on:
  workflow_dispatch:
  schedule:
    # GitHub-hosted scheduled jobs can be delayed or occasionally miss runner
    # assignment. Use staggered daily recovery slots and let the update script
    # commit only the first successful scheduled run for each Chicago date.
    - cron: "7 10,11,12,13,14,16,18,20 * * *"

concurrency:
  group: update-birthday-messages
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-birthday-messages:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Sync with latest main
        run: git pull --ff-only origin main

      - name: Generate shortcut message
        env:
          FORCE_SHORTCUT_UPDATE: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
        run: node scripts/update-shortcut-message.mjs

      - name: Generate terminal page
        env:
          FORCE_TERMINAL_UPDATE: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
        run: node scripts/update-terminal-page.mjs

      - name: Commit updated generated messages
        run: |
          if git diff --quiet -- shortcut-message.txt terminal.txt; then
            echo "No generated message changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add shortcut-message.txt terminal.txt
          git commit -m "Update generated birthday messages"
          git push origin HEAD:main
